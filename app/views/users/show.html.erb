<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @user.name %>
</p>

<p>
  <b>Email:</b>
  <%= @user.email %>
</p>

<% @user.microposts.each do |micropost| %>
    <hr />
    <tr>
      <td><%= micropost.content %></td>
      <td><%= link_to 'Show Post', micropost %></td>
    </tr>
    <hr />
<% end %>
<br />


<%= link_to 'Edit User', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
